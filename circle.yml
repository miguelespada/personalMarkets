test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) rake rspec ;; 1) rake cucumber ;; esac:
        parallel: true
deployment:
  staging:
    branch: master
    heroku:
      appname: magdalenasfederico
machine:
  services:
    - elasticsearch
