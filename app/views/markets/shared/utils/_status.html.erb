<% if can?(:manage, market) %>
  <section id="messages">
  
    <%if market.state = "published" %>
      <div class="alert alert-warning flash"> This market is not public. Publish it or edit it add more content (<%= link_to "+info", :anchor => "market-quality" %>)
      </div>
    <% end %>

  
  </section>
<% end %>