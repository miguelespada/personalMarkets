
<% prevDate = nil %>
<% market.human_readable_schedule.each do |day| %>
    <% if prevDate && prevDate == day["date"] %>
      <%= append_date(day) %> 
    <% else %>
     </br>  <%= format_date(day) %> 
    <% end %>
    <%  prevDate = day["date"] %>
<% end %>