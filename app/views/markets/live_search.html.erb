 <%= market_list(@markets, "gallery") %>
  
<div>
  <%= link_to "Prev", "#", :id=>"prev-markets" if !@first_page %>
  <%= link_to "Next", "#", :id=>"next-markets" if !@last_page %>
</div>

<script>
 $("#prev-markets").click(function(){
    var page = parseInt($('#page').val());
    $('#page').val(page - 1);
    $('form').change();
  });

  $("#next-markets").click(function(){
    var page = parseInt($('#page').val());
    $('#page').val(page + 1);
    $('form').change();
  });
</script>