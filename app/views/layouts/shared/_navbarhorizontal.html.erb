 <!-- Navbar Horizontal START -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to "Personal Markets", "/", :class => "navbar-brand"   %>
        </div>

        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
                <% if user_signed_in? %>
                <li><%= link_to "Add market", new_user_market_path(current_user) %></li>
                <li><%= link_to "My markets", user_markets_path(current_user) %></li>
                <li><%= link_to "My favorites", user_path(current_user) %></li>
                <% end %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <% if user_signed_in? %>
                <li><span class="badge"><span class="glyphicon glyphicon-user"></span> You are logged as: <%= current_user.email %></span></li>
              <% end %>
                <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">User account<b class="caret"></b></a>
            <ul class="dropdown-menu" role="menu">
              <% if user_signed_in? %>
                <li><%= link_to "Sign out", destroy_user_session_path %></li>
              <% else %>
                <li><%= link_to "Sign in", new_user_session_path %></li>
                <li><%= link_to "Sign up", new_user_registration_path %></li>
              <% end %>
            </ul>
        </div>
    </div>
</nav>
 <!-- Navbar Horizontal  STOP -->