<%= page_title(t(:admin_panel), "fa-cog") %>

<div class="container main-container">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-8 col-md-offset-2 center">

        <%= admin_link(Market) %>

        <%= admin_link(Category) %>

        <%= admin_link(SpecialLocation) %>

        <%= admin_link(Tag) %>
        
        <%= admin_link(Wish) %>

        <%= admin_link(Bargain) %>

        <%= admin_link(Photo) %>

        <%= external_link("https://mail.google.com", t(:messages).capitalize, "fa-envelope-o") %>

        <%= admin_link(User) %>

        <%= current_user_dashboard_link("show_statistic_path", t(:stats).capitalize, "fa-tasks") %>

        <%= admin_link(SlideshowImage) %>

        <%= admin_link(SlideshowText) %>

        <%= external_link("https://app.paymill.com/es-es/auth/login", "Paymill", "fa-money") %>

        <%= admin_link(Coupon) %>

        <%= current_user_dashboard_link("settings_path", t(:admin_data).capitalize, "fa-cogs") %>

        <%= current_user_dashboard_link("last_transactions_path", "Digest", "fa-database") %>

      </div>
    </div>
  </div>
</div>