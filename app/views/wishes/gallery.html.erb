<h1>Make people's wishes come true, recommend markets.</h1>

<div class="paginate">
  <%= paginate @wishes %>
</div>

<div class = "wish-gallery row">
  <% @wishes.each do |wish| %>
    <%= render "wishes/show", :wish => wish %>
  <% end %>
</div>


